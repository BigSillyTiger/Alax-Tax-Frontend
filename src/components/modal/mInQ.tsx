import React from "react";
import ModalFrame from "./modalFrame";

const MInQ = () => {
    return (
        <>
            <ModalFrame open={} onClose={} title={} mode="full" />
        </>
    );
};

export default MInQ;
