import React from "react";
import { Dialog, Transition } from "@headlessui/react";

// this component is about building a modal with transition to update a client
const MUpdateClient = () => {
    return <></>;
};

export default MUpdateClient;
